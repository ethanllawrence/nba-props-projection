# Local pipeline state (regenerated by `python -m nproj init` / daily).
# Leading slash = top-level folders only; docs/data/ is the live site and MUST be committed.
/data/
/models/
*.db
*.sqlite

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Secrets
.env

# OS / editor
.DS_Store
Thumbs.db
desktop.ini
.vscode/
